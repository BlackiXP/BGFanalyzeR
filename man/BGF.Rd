% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{BGF}
\alias{BGF}
\alias{from_AMPTSV2_report}
\alias{from_standard_record}
\title{Helper to set up a \code{BGF} object}
\usage{
BGF(
  ReactorLayout,
  BlankLabel = "Blank",
  name = "new_BGF",
  ProcessTemp = NA,
  InocToSubRatio = 2,
  MeasurementType = NA
)

from_AMPTSV2_report(
  ReactorLayout,
  BlankLabel = "Blank",
  name = "new_BGF",
  ProcessTemp,
  InocToSubRatio,
  path,
  feedback = FALSE
)

from_standard_record(
  ReactorLayout,
  ProcessTemp,
  InocToSubRatio,
  path,
  time_col,
  product_col,
  BlankLabel = "Blank",
  name = "new_BGF",
  units = "hours",
  RName = "R1",
  feedback = FALSE,
  ...
)
}
\arguments{
\item{ReactorLayout}{A character vector providing the reactor layout, e.g. the grouping factor used for plotting and yield calculation of fermetation(s) in a BGF object}

\item{BlankLabel}{A character string indicating which group in ReactorLayout is the inoculum used for net gas/ yield calculation}

\item{name}{A character vector specifying the name of the new BGF object}

\item{ProcessTemp}{The process temperature of the fermentation(s) to be stored in the BGF object}

\item{InocToSubRatio}{The inoculum to substrate ratio (= inoculation strength) of the fermentation(s) to be stored in the BGF object}

\item{MeasurementType}{An optional character string indicating the measuremet type of the BGF object to be created}

\item{path}{A path pointing an external data input file must be. Must be in a tidy format for from_standard_record or a report generated by an AMPTS II (see Details)}

\item{feedback}{\code{Logical} if \code{TRUE} the function prints a summary to the R console}

\item{time_col}{numeric. Indicates the position of the time stamp column in the imported report}

\item{product_col}{numeric. Indicates the position of the product column (e.g. cummulative biogas volume) in the imported report}

\item{units}{character string.  Units in which the results are
    desired.  Can be abbreviated.}

\item{RName}{character. Label to be added to the data of the imported report. Should match a value of \code{ReactorLayout}}

\item{...}{further arguments passed to read.table}
}
\value{
A \code{BGF} object
}
\description{
Three helper functions exist that allows users to set up \code{BGF} objects either from scratch or from external data files.
}
\details{
All three helper functions internally call \code{new_BGF()} with distinct parameters arguments pre set.
The easiest way to generate a \code{BGF} object is the \code{BGF()} function. It only needs the \code{ReaktorLayout} argument to be specified by the user and will subsequently create a object of class \code{BGF}.

If \code{from_AMPTSV2_report} is used, \code{path} must point to the \code{report_yyyy-mm-dd_HHMM.csv}-file created by an AMPTS II (BioProcess Control; Lund; Sweden).
This special feature was included as the AMPTS II is widely distributed among biogas labs and several R tutorials exist online, that also refer on this system.

When \code{from_standard_record} is used to create the \code{BGF} object any text file storing biogas fermentation data could serve as a template.
It is a wrapper to call \code{read.table} with certain arguments pre-set.
These are: dec=".", sep="\\t" and header=TRUE.

Such a text file must have a matrix like structure in which each row represents a unique observation.
It must have at least two columns. One with the time stamp of the observation (format: \verb{\%y-\%m-\%d \%H:\%M:\%S}).
The other one must be an accumulating volume measurement of exhaust gas (biogas) at this moment.
Further columns providing additional information (such as pH, temperature or RedOx potential) can be present and will be imported as well.
}
\examples{
# create a simple BGF without data
BGF(LETTERS[c(1:5)])

# create a BGF based on the report of an AMPTS II
from_AMPTSV2_report(
        ReactorLayout = c("2*Blank","Cellulose","3*neg ctrl","3*FR1","3*FR2","3*FR3"),
        BlankLabel = "Blank",
        name = "Test",
        InocToSubRatio=2,
        ProcessTemp = 52,
        path = base::system.file("extdata","AMPTSV2.csv",package = "BGFanalyzeR"))

# create a BGF from a minimal external data file
from_standard_record(ReactorLayout = "A",
        ProcessTemp = 80,
        InocToSubRatio = .1,
        path = base::system.file("extdata","Fermentation_A.tsv",package = "BGFanalyzeR"),
        time_col = 1,
        product_col = 3)

}
