% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_import.R
\name{import_standard_record}
\alias{import_standard_record}
\alias{read_raw_AMPTSV2_report}
\alias{import_BGF_object}
\title{Data import functions}
\usage{
import_standard_record(
  ipath,
  dec = ".",
  sep = "\\t",
  header = TRUE,
  mkFRTime = NULL,
  FRTime_col = NULL,
  units = NULL,
  ...
)

read_raw_AMPTSV2_report(path, sub = "\\\\\\"")

import_BGF_object(path)
}
\arguments{
\item{ipath, path}{a path pointing to an external file}

\item{dec}{the character used in the file for decimal points.}

\item{sep}{the field separator character.  Values on each line of the
    file are separated by this character.  If \code{sep = ""} (the
    default for \code{read.table}) the separator is \sQuote{white space},
    that is one or more spaces, tabs, newlines or carriage returns.}

\item{header}{a logical value indicating whether the file contains the
    names of the variables as its first line.  If missing, the value is
    determined from the file format: \code{header} is set to \code{TRUE}
    if and only if the first row contains one fewer field than the
    number of columns.}

\item{mkFRTime}{NULL by default. Can be a character string representing a start date (\%y-\%m-\%d \%H:\%M:\%S) for the calculation of the fermentation time}

\item{FRTime_col}{NULL by default. Can be a character string with the name, or an integer representing the position of the date column after the input file was read via read.table.}

\item{units}{character string.  Units in which the results are
    desired.  Can be abbreviated.}

\item{...}{Further arguments to be passed to \code{read.table}.}

\item{sub}{a \code{character}, which will be eliminated from the read in text connection}
}
\value{
Either a data.frame, a list or a BGF
}
\description{
The \code{BGFanalyzeR} package has three data import functions.
Two are called internally by the helper functions \link[=BGF]{from_standard_report} or \link[=BGF]{from_AMPTV2_report} when creating a new \code{BGF} object.
The third allows to import a \code{BGF} object, that was previously exported from R.
}
\details{
Two of the three data import functions, \code{import_standard_record} and \code{read_raw_AMPTSV2_report} are unlikely to be directly called by a package user.
Instead, they are called by \code{from_standard_record} or \code{from_AMPTSV2_report}, respectively, when importing an external data file.

The first import function \code{import_standard_record} is more than a wrapper for \link{read.table} with the arguments dec=".", sep="\\t" and header=TRUE pre set.
It furthermore allows to calculate a fermentation time directly when importing the data.
To this end, \code{mkFRTime} must be a \code{character} string representing a date in the format \%y-\%m-\%d \%H:\%M:\%S, \code{FRTime_col} an \code{integer} specifying the position of the time stamp within the data, and \code{units} must be a \code{character} specifying the desired \link[=difftime]{units} of the calculated fermentation time.

In case of \code{import_standard_record} a \code{data.frame} is returned.

The function \code{read_raw_AMPTSV2_report} calls \link{readLines} and expects a relative \code{path} to a \code{report_yyyy-mm-dd_HHMM.csv}-file generated by the AMPTS II web interface (Login > Download report > Generate report > Download generated report as raw text file (CSV)).
\strong{The function expects the original file generated by the AMPTS II}, NOT a .CSV version previously opened and saved by other software as this will replace the original AMPTS II-generated formatting the downstream function is build on.
The argument \code{sub} is used in a call to \link{gsub}, which is needed to eliminate an artifact character introduced by calling \code{readLines}.
Upon artifact elimination the raw data is converted into a \code{list} of three:

\strong{ExpPara}: A \code{list}. Information concerning the all reactors part of the AMPTS II experiment. Serves as template for \code{ExpParam} of a \code{BGF}

\strong{ExpSetup}: A \code{data.frame}. Information on individual reactors being part of the AMPTS II experiment. All information collected here will be added to metaData of a BGF

\strong{ExpData}: A \code{data.frame}. Biogas volumes and flow data of the AMPTS II experiment. This data will be moved to \code{BioGasData} of a \code{BGF}

This list is returned by \code{read_raw_AMPTSV2_report}.

The third data import function, \code{import_BGF_object} allows to import a \code{BGF} from either an \link[=saveRDS]{.RDS-file} or an .csv-file as produced by \link{save_BGF}.
A detailed format description of the .csv-file this function can read can be found \link[=save_BGF]{elsewhere}.
The function expects a \code{path} to a file as a single argument.
It will check the ending of the file path.
If it's '.csv', a \code{BGF} is rebuild based on the read in data.
Else the function  serves as a wrapper to \code{readRDS}.
Consequently, a \code{BGF} is returned.
}
\examples{
# import biogas fermentation from a .tsv file
stRep <- import_standard_record(
      ipath = base::system.file("extdata","Fermentation_B.tsv",package ="BGFanalyzeR"),
      header=TRUE,
      dec=".",
      sep="\t")

# calculate a fermentation time while importing the data
stRep_frt <- import_standard_record(
        ipath = base::system.file("extdata","Fermentation_B.tsv",package ="BGFanalyzeR"),
        header=TRUE,
        dec=".",
        sep="\t",
        mkFRTime = "2026-01-29 23:00:00",
        FRTime_col = 2,
        units = "hours")


# create a list that can be used as a template to build a BGF
RawReport <- read_raw_AMPTSV2_report(
        path = base::system.file("extdata","AMPTSV2.csv",package ="BGFanalyzeR"))



# import a BGF from a '.csv'-file
BGF_csv <- import_BGF_object(
      path = base::system.file("extdata","importable_BGF_object.csv",package = "BGFanalyzeR"))

# import a BGF from a '.RDS'-file
BGF_rds <- import_BGF_object(
      path = base::system.file("extdata","importable_BGF_object.RDS",package = "BGFanalyzeR"))

}
