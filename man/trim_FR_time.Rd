% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/move_fermentations.R
\name{trim_FR_time}
\alias{trim_FR_time}
\title{Reorientate fermentations of a \code{BGF} by time}
\usage{
trim_FR_time(x, value, mode = "all", left_end = TRUE, cut_left = TRUE)
}
\arguments{
\item{x}{a \code{BGF}}

\item{value}{a \code{numeric}; either the time before or after which data points should be removed (or shifted)}

\item{mode}{a \code{character}. Can be 'all' to apply the trimming to all fermentations of a \code{BGF}. Otherwise each element in \code{mode} must refer to a fermentation label (e.g. 'mode = c("R1","R3","R4","R15")' to trim fermentation R1, R3, R4 and R15)}

\item{left_end}{\code{logic}; default \code{TRUE}. Should the fermentation/ observation time be shifted towards the left end? If \code{FALSE} data points later than the selected 'value' will be removed}

\item{cut_left}{\code{logic}; default \code{TRUE}. Should data points with a negative fermentation/ observation times be removed?}
}
\value{
a \code{BGF}
}
\description{
The function allows to remove data points of individual or all fermetations based on the fermetation/ observation time ('time' column in \code{BiGasData} layer of a \code{BGF}).
The user can either remove data points later than a specified 'time' value, or substract a specified value from each value in the 'time' column and afterwards optionally remove the resulting times < 0.
}
\examples{
# create example BGF
myBGF <- from_AMPTSV2_report(
        ReactorLayout = c("2*Meso","Cellulose","2*S1 ctrl","2*S1 7d","2*S1 4d",
                          "2*S2 ctrl","2*S2 4d","2*S2 6d"),
        BlankLabel = "Meso",
        name = "myBGF",
        ProcessTemp = 42,
        path = base::system.file("extdata","AMPTSV2.csv",package = "BGFanalyzeR"),
        InocToSubRatio = 2,
        feedback = TRUE)

# BioGasData has 735 rows
nrow(myBGF$BioGasData)

# remove all data before day 30
myBGF <- trim_FR_time(myBGF,value= 30)

# BioGasData has now only 285 rows
nrow(myBGF$BioGasData)

# remove all data after day 15
myBGF <- trim_FR_time(myBGF,value= 15,left_end=FALSE,cut_left=FALSE)

# BioGasData has now only 225 rows
nrow(myBGF$BioGasData)

}
