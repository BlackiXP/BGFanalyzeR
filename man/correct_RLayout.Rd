% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_correction.R
\name{correct_RLayout}
\alias{correct_RLayout}
\alias{cols_to_numeric}
\title{Data correction functions}
\usage{
correct_RLayout(lo)

cols_to_numeric(x, vec = c(2:4), layer = "BioGasData")
}
\arguments{
\item{lo}{a \code{character} specifying a reactor layout (via the 'ReactorLayout' argument of certain \code{BGFanalyzeR} functions)}

\item{x}{a \code{BGF}}

\item{vec}{either a \code{character} of column names or an \code{integer} indicating the positions of columns in the chosen \code{layer}}

\item{layer}{a \code{character} specifying a layer of a \code{BGF}. Can be 'metaData' or 'BioGasData'}
}
\value{
a \code{character} (\code{correct_RLayout})

a \code{BGF} (\code{cols_to_numeric})
}
\description{
A set of functions that provide shortcuts for frequently used operations conducted on a \code{BGF}.
For instance, they allow to specify the reactor layout in a formula like manner or to change a the type of any column of a \code{BGF}'s \code{metaData} or \code{BioGasData} layer to \code{numeric}.
}
\details{
The function \code{correct_RLayout} allows to write 'ReactorLayout=c("3\code{*}Blank","3\code{*}Cellulose","3\code{*}Treatment A")' instead of 'ReactorLayout=c("Blank","Blank","Blank","Cellulose","Cellulose","Cellulose","Treatment A","Treatment A","Treatment A")' or 'ReactorLayout=c(rep("Blank",3),rep("Cellulose",3),rep("Treatment A",3))'.

The function will recognize the '\*' and expects an \code{integer} on the left side specifying how often the right side should be repeated.

It is useful when dealing with replicates among the fermentations of a \code{BGF}, as it can be used to add redundant information to the 'metaData' layer of a (see examples).

The function \code{cols_to_numeric} allows to change the type of any column in the \code{metaData} or \code{BioGasData} layer of a \code{BGF} to \code{numeric}.
It is possible to apply this change to several columns of the same layer at once.
}
\examples{
# define a reactor layout
RL <- c("3*Blank","3*Cellulose","3*Treatment A")

# correct reactor layout
RL_cor <- correct_RLayout(RL)

RL_cor # print corrected layout

# example code
myBGF <- BGF(RL)

# add dry total solutes concentration to 'metaData'
myBGF<-add_metaData(x = myBGF,what = correct_RLayout(c("3*2.9","3*98.7","3*8.4")),lab="TS")

# change the new 'TS' column in 'metaData' layer to numeric
myBGF <- cols_to_numeric(myBGF,"TS","metaData")

}
