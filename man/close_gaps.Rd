% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_values.R
\name{close_gaps}
\alias{close_gaps}
\alias{na_correction}
\title{Close gaps in data}
\usage{
close_gaps(x, feedback = FALSE)

na_correction(x, which = "all_num", ...)
}
\arguments{
\item{x}{a \code{BGF}}

\item{feedback}{\code{logic}. If \code{TRUE} the function will print a feedback to the console}

\item{which}{either a \code{numeric} or a \code{character}referring to \code{numeric} columns in the \code{BioGasData}-layer of a \code{BGF}. Can be 'all_num' (the default) to automatically select all \code{numeric} columns in the \code{BioGasData}-layer}

\item{...}{further arguments passed to \link{bgf_interpolation}}
}
\value{
a \code{BGF}
}
\description{
Two functions exist, that can be used to close gaps in the \code{BioGasData} layer of a \code{BGF}.
Depending on the function, used either the last valid value of a fermentation is carried forward or values in between two measurements can be interpolated.
}
\details{
The function \code{close_gaps} can be used to quickly close gaps at the end of a series of fermentations.
It will go through each 'time' in the \code{BioGasData} layer of a \code{BGF} and check if the corresponding 'product' and 'production' values are \code{NA}.
If \code{TRUE} the respective previous value will be selected and replaces the \code{NA}.
This action will be done specifically for each 'reactor' level.
It is called by  \link{from_AMPTSV2_report} when creating a \code{BGF} from an AMPTS II generated report.

The function \code{na_correction} can be used to close gaps in between or at the end of a fermentation.
Target data columns can be specified as \code{integer} or \code{character} in the \code{which} argument.
Alternatively, \code{which} can be 'all_num' (the default) to select all numeric columns in the \code{BioGasData}-layer of a \code{BGF}.
Internally the function calls \link{bgf_interpolation} on each column sepcified via the \code{which} argument.
Additional arguments passed to \code{na_correction} will be forwarded to \link{bgf_interpolation} as well.
}
\examples{
# create an example BGF
myBGF <- BGF(
        ReactorLayout = c("2*Meso","Cellulose","2*S1 ctrl","2*S1 7d","2*S1 4d",
        "2*S2 ctrl","2*S2 4d","2*S2 6d"),
        BlankLabel = "Meso",
        name = "myBGF",
        ProcessTemp = 42,
        MeasurementType = "AMPTSV2")

# add data generated by an AMPTS II
myBGF <- add_bmp_measurement(
        x = myBGF,
        path = base::system.file("extdata","AMPTSV2.csv",package = "BGFanalyzeR"))

# convert data columns
myBGF <- cols_to_numeric(myBGF)

# close gaps in data
myBGF <- close_gaps(myBGF)

# another example BGF
myBGF2 <- from_standard_record(
        ReactorLayout = "A",
        ProcessTemp = 80,
        InocToSubRatio = .1,
        path = base::system.file("extdata","Fermentation_A.tsv",package = "BGFanalyzeR"),
        time_col = 1,
        product_col = 3)

# import gas quality measurements
gasq <- import_standard_record(
        ipath = base::system.file(
             "extdata",
             "gasq_A.tsv",
             package = "BGFanalyzeR"),
        mkFRTime = "2025-01-15 17:00:00",
        FRTime_col = 1,
        units = "hours")

# add gas quality data to BGF
myBGF2 <- add_BG_parameter(
        x = myBGF2,
        parameter = gasq,
        reactor= "R1",
        time = 3,
        value = 2,
        name = "H2",
        cut_zero = TRUE,
        interpolate_missing = FALSE)

# ensure data structure integrity (recommended before using na_correction)
myBGF2 <- update_BGF(myBGF2)

# close gaps resulting from merging fermentation data and gas quality data
# in all numerics of the BGF's 'BioGasData'-layer
myBGF2 <- na_correction(x=myBGF2)

}
