% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsetting_BGF.R
\name{subset_BGF}
\alias{subset_BGF}
\alias{merge_BGF}
\title{Split or merge \code{BGF}'s}
\usage{
subset_BGF(x, reactor = NULL, layout = NULL, name = NULL)

merge_BGF(x, y, mergeParam = FALSE, x_tag = NULL, y_tag = NULL, name = NULL)
}
\arguments{
\item{x, y}{a \code{BGF}}

\item{reactor}{defaults to \code{NULL}. Can be an \code{integer} or \code{character} specifying the reactors/ fermentations to keep based on the reactor name (= row name of the fermentation in \code{metaData}-layer)}

\item{layout}{defaults to \code{NULL}.Can be an \code{integer} or \code{character} specifying the reactors/ fermentations to keep based on the reactor layout (= respectiv vaslue in the 'Layout' column  of the \code{metaData}-layer)}

\item{name}{defaults to \code{NULL}. Can be a \code{character} providing a new name for the \code{BGF}}

\item{mergeParam}{\code{logic}; defaults to \code{FALSE}. Should the \code{ExpParam}-layer of the \code{BGF}'s be also merged?}

\item{x_tag, y_tag}{defaults to \code{NULL}. Can be a \code{character} which is used as a tag when merging \code{ExpParam}-layers}
}
\value{
a \code{BGF}
}
\description{
Functions that allow sub-setting and merging of `BGF``'s.
}
\details{
The function \code{subset_BGF} can be used to subset a \code{BGF}.
The user can either use reactor/ fermentation names (= row names of \code{metaData}-layer, values in column 'reactor' of \code{BioGasData}-layer), or reactor layouts (= values in 'Layout' column of \code{metaData}-layer) as a basis for subsetting.
Furthermore, it is possible to give a new name to the resulting subset.

The function \code{merge_BGF} can be used to merge two \code{BGF}'s.
On the way, it will generate new row names for the \code{metaData}-layer and adjust the values in the 'reactor' column of the \code{BioGasData}-layer appropriately.

In general, it is advised to only merge \code{BGF}'s that have the same values in their \code{ExpParam}-layer, as with the default setting only the \code{ExpParam}-layer of the first \code{BGF} passed to \code{merge_BGF} is preserved.
It is possible to change the default behavior with the 'mergeParam' argument.
In that case, new names for entries in the merged \code{ExpParam}-layer will be generated if a entry is either present in only one \code{ÈxpPram}-layer of the \code{BGF}'s or if the values for the same entry in both \code{BGF}'s differs.
The new names are prolonged by a specific tag (controlled via arguments 'x_tag' and 'y_tag'), so that it is clear where that entry originates from.

Furthermore, these tags will be added to the \code{metaData}- and \code{BioGasData}-layer.
}
\examples{
# create an example BGF
myBGF <- from_AMPTSV2_report(
        ReactorLayout = c("2*Blank","Cellulose","2*S1 ctrl","2*S1 7d","2*S1 4d",
                          "2*S2 ctrl","2*S2 4d","2*S2 6d"),
        BlankLabel = "Blank",
        name = "myBGF",
        InocToSubRatio=2,
        ProcessTemp = 52,
        path = base::system.file("extdata","AMPTSV2.csv",package = "BGFanalyzeR"))
myBGF

# subset using 'reactor' argument
myBGF_subset1 <- subset_BGF(myBGF,reactor=c(4:9),name="subset 1")
myBGF_subset1

# subset using 'layout' argument
myBGF_subset2 <- subset_BGF(myBGF,layout=c("S2 ctrl","S2 4d","S2 6d"),name="subset 2")
myBGF_subset2

# merge the BGF's
mergedBGF <- merge_BGF(myBGF_subset1,myBGF_subset2,TRUE)
mergedBGF

}
