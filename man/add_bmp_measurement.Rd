% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support_data_import.R
\name{add_bmp_measurement}
\alias{add_bmp_measurement}
\alias{add_standard_record}
\alias{add_ExpPara}
\alias{add_ExpSetup}
\alias{sort_AMPTSV2_reactors}
\alias{sort_standardReport}
\title{Supportive data import functions}
\usage{
add_bmp_measurement(x, path, mode = "auto", feedback = FALSE)

add_standard_record(
  x,
  path,
  header = TRUE,
  dec = ".",
  sep = "\\t",
  units = "hours",
  time_col = "time",
  RName = "R1",
  product_col = "GCounter..ml.",
  feedback = TRUE
)

add_ExpPara(x, rawReport, feedback = F)

add_ExpSetup(x, rawReport, feedback = F)

sort_AMPTSV2_reactors(x, rawReport, feedback = F)

sort_standardReport(x, rawReport, RName, product_col)
}
\arguments{
\item{x}{a \code{BGF}}

\item{path}{a \code{path} pointing to an external data file. Either a \code{.csv}-file generated by the AMPTS II (BioProcess Control; Lund; Sweden), or a text-file with at least biogas production (cumulative volume) and time data.}

\item{mode}{the mode \code{add_bmp_measurement} treats the imported data at path. Currently only 'auto' and 'AMPTSV2' are the only implemented options.}

\item{feedback}{\code{logic}; if \code{TRUE} the function will print a textual feedback to the console}

\item{header}{\code{logic}; does the matrix like structure at \code{path} has a \code{header}}

\item{dec}{a single \code{character} specifying the decimal separator}

\item{sep}{a single \code{character} specifying the column separator}

\item{units}{the desired unit of the time differnce calculated by \code{calc_FR_time}}

\item{time_col}{a \code{character} specifying the name of the column with time information in the external data file}

\item{RName}{a \code{character} specifying the reactor name of the biogas fermentation to be added. Defaults to 'R1'}

\item{product_col}{a \code{character} specifying the name of the column with the cumulative biogas volume data in the external data file}

\item{rawReport}{either a \code{list} or a \code{data.frame} created by \code{read_raw_AMPTSV2_report} or \code{import_standard_record}, respectively.}
}
\value{
a \code{BGF}
}
\description{
A set of internal functions that bridge the data import and object creation when using \link{from_AMPTSV2_report} or \link{from_standard_record} to create an \code{BGF}.
These functions are not meant for direct user interaction.
}
\details{
The function \code{add_bmp_mesurement} is called by \link{from_AMPTSV2_report}.
It imports a \code{.csv}-file created by the AMPTS II (BioProcess Control; Lund; Sweden) from a location on the system specified in \code{path} and adds it to a \code{BGF} specified in \code{x}.

Internally, \code{add_bmp_mesurement} calls \code{read_raw_AMPTSV2_report} to import the external file to a list and then it calls \code{add_ExpPara}, \code{add_ExpSetup} and \code{sort_AMPTSV2_reactors} to transfer the data to the \code{BGF}.

The function \code{add_standard_record} is similar to \code{add_bmp_measurement}, put in this case \code{path} must point to an external text file with a matrix like data structure.
This matrix must have at least two columns, one with information on when the measurement was recorded (\code{time_col}), the other with a cumulative biogas volume measurement (\code{product_col}).
Additional columns can be present and will be imported as well.

Internally, \code{add_standard_record} calls \link{import_standard_record} to import the data and next it calls \link{calc_FR_time} to calculate a standardized fermentation time for the imported data set.
Finally, it calls \code{sort_standardReport} to transfer the imported data to the BGF
The function will not remove empty rows resulting from object creation within the BGF.
Thus, it is adviced to use \link{update_BGF} to ensure the integrity of the \code{BGF}.

The function \code{add_ExpPara} is called by \code{add_bmp_measurement} and acts as a wrapper for \link{add_ExpParam} to transfer experimental meta data to a \code{BGF} object.
It expects a \code{list} created by \link{read_raw_AMPTSV2_report} as a second argument.

Similarly,the function \code{add_ExpSetup} is also called by \code{add_bmp_measurement}, but acts as a wrapper for \link{add_metaData} to transfer fermentation meta data to a \code{BGF} object.
Consequently it also expects a \code{list} created by \link{read_raw_AMPTSV2_report} as a second argument.

In addition, \code{sort_AMPTSV2_reactors} is also called by \code{add_bmp_measurement} and acts as a wrapper for \link{add_BG_measurement} and \link{alter_BG_measurement} to transfer cumulative biogas volume and biogas flow data to a \code{BGF}.
Like the former two functions, it expects a \code{list} generated via \code{read_raw_AMPTSV2_report} as second argument

Additional details...
}
\examples{
# create an example BGF
myBGF <- BGF(ReactorLayout = c("2*Blank","Cellulose","3*neg ctrl","3*FR1","3*FR2","3*FR3"),
              name = "myBGF",
              ProcessTemp = 52,
              MeasurementType = "AMPTSV2")

myBGF # print the BGF

# add data from external files
myBGF <- add_bmp_measurement(
        x = myBGF,
        path = base::system.file("extdata","AMPTSV2.csv",package = "BGFanalyzeR"))

myBGF # print the BGF

# create another example BGF
myBGF2 <- BGF("A")

myBGF2 # print the BGF

# add data from an external file (standard record)
myBGF2 <- add_standard_record(
        x = myBGF2,
        path = base::system.file("extdata","Fermentation_B.tsv",package = "BGFanalyzeR"),
        RName = "R1",
        time_col = "UTC",
        product_col = "GCounter..ml.")

myBGF2 # print the BGF

# create an additional example BGF
myBGF3 <- BGF(c("2*Blank","Cellulose","3*neg ctrl","3*FR1","3*FR2","3*FR3"))

myBGF3 # print the BGF

# import standard record
rawReport <- read_raw_AMPTSV2_report(
        path = base::system.file("extdata","AMPTSV2.csv",package = "BGFanalyzeR"))

# add experimental meta data to BGF
myBGF3 <- add_ExpPara(myBGF3,rawReport,TRUE)

myBGF3 # print the BGF

# add fermentation meta data to BGF
myBGF3 <- add_ExpSetup(myBGF3,rawReport,TRUE)

myBGF3 # print the BGF

# add biogas volume and flow data
myBGF3 <- sort_AMPTSV2_reactors(myBGF3,rawReport)

myBGF3 # print the BGF

# create an empty example BGF
myBGF4 <- BGF("myBGF4")

myBGF4 # print the BGF

# import a standard record
rawReport2 <- import_standard_record(
        ipath = base::system.file("extdata","Fermentation_B.tsv",package = "BGFanalyzeR"))

# calculate a fermentaion/ observation time
rawReport2<-calc_FR_time(rawReport2,1,units="hours")

# add imported data to BGF
myBGF4 <- sort_standardReport(myBGF4,rawReport2,"R1",3)

myBGF4 # print BGF

}
