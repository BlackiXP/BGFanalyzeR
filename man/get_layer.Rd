% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_functions.R
\name{get_layer}
\alias{get_layer}
\alias{get_whatever}
\alias{get_MeasurementType}
\alias{get_ReactorLayout}
\alias{get_BlankLabel}
\alias{get_Excluded}
\alias{get_blanks}
\alias{get_yield_summary}
\title{Extract information from a BGF}
\usage{
get_layer(x, layer, feedback = F)

get_whatever(x, layer, what, feedback = F)

get_MeasurementType(x, feedback = F)

get_ReactorLayout(x, feedback = F)

get_BlankLabel(x, feedback = F)

get_Excluded(x, feedback = F)

get_blanks(x, feedback = F)

get_yield_summary(x, Excluded = F, feedback = T)
}
\arguments{
\item{x}{a \code{BGF}}

\item{layer}{a layer of a \code{BGF}; either 'ExpParam', 'metaData' or 'BioGasData'}

\item{feedback}{\code{logic}, if \code{TRUE} the function will print a feedback to the R console}

\item{what}{a \code{character} referring either to a list entry of the \code{ExpParam} layer, or a column of the \code{metaData} or \code{BioGasData} layer of a \code{BGF}}

\item{Excluded}{\code{logic} if \code{TRUE}, excluded reactors are not included in the summary. Default = \code{FALSE}}
}
\value{
\code{get_layer} returns a either a \code{data.farme} or \code{list}

\code{get_whatever} returns a either a \code{data.farme} column or \code{list} entry

\code{get_MeasurementType} returns a \code{character}

\code{get_ReactorLayout} returns a \code{factor}

\code{get_BlankLabel} returns a \code{factor}

\code{get_Excluded} returns a \code{character}

\code{get_blanks} returns a \code{character}

\code{get_yield_summary} returns a \code{data.frame}
}
\description{
A set of functions that can be used to extract various information from a \code{BGF}.
}
\details{
The functions \code{get_MeasurementType}, \code{get_ReactorLayout}, \code{get_BlankLabel},\code{get_Excluded} and \code{get_blanks} are wrapper for \code{get_whatever} and extract frequently needed information from a \code{BGF}.

The function \code{get_whatever} internally calls \code{get_layer} to extract any layer from a \code{BGF}, and subsequently return only a single \code{list} entry or a \code{data.frame} column.

The function \code{get_yield_summary} returns a \code{data.frame} if a yield summary was generated for the \code{BGF} via \code{summarize_yield}.
}
\examples{
# create an example BGF
myBGF<-BGF(LETTERS[1:5],"A","myBGF",52,2,"manuel")

# extract 'ExpParam' layer
ExpParam <-get_layer(myBGF,"ExpParam",TRUE)

# extract 'metaData' layer
metaData <-get_layer(myBGF,"metaData",TRUE)

# extract 'BioGasData' layer
BioGasData <-get_layer(myBGF,"BioGasData",TRUE)

# extract 'name'-attribute
get_whatever(myBGF,"ExpParam","name")

# extract 'MeasurmentType'-attribute
get_MeasurementType(myBGF)

# extract the reactor layout
get_ReactorLayout(myBGF)

# extract the Blank label
get_BlankLabel(myBGF)

# Exclude reactor 1 and 5
myBGF$metaData$Excluded[c(1,5)] <- TRUE

# extract the excluded reactors
get_Excluded(myBGF)

# extract names of blank reactors
get_blanks(myBGF)

# create a second example BGF
myBGF2<-from_AMPTSV2_report(
        ReactorLayout = c("2*Blank","Cellulose","3*neg ctrl","3*FR1","3*FR2","3*FR3"),
        BlankLabel = "Blank",
        name = "myBGF2",
        InocToSubRatio = 2,
        ProcessTemp = 52,
        path = base::system.file("extdata","AMPTSV2.csv",package = "BGFanalyzeR"))

# extract yield summary
get_yield_summary(myBGF2)

}
