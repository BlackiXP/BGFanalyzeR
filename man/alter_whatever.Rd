% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alter_functions.R
\name{alter_whatever}
\alias{alter_whatever}
\alias{alter_BG_measurement}
\title{Alter data within a \code{BGF}}
\usage{
alter_whatever(x, layer, what, value, ID = NULL, feedback = F)

alter_BG_measurement(
  x,
  reactor_id,
  time_id,
  col,
  measurement,
  ID = NULL,
  feedback = F
)
}
\arguments{
\item{x}{a \code{BGF}}

\item{layer}{a \code{character} referring a layer of a \code{BGF}; either 'ExpParam' or 'metaData'}

\item{what}{a \code{character} referring either to a list entry of the \code{ExpParam} layer, or a column of the \code{metaData} layer of a \code{BGF}}

\item{value, measurement}{either a \code{character}, \code{numeric} or \code{logic}. The desired new value}

\item{ID}{defaults to \code{NULL}, for \code{alter_whatever} can be a \code{character} or \code{integer} referring to row names in the \code{metaData} layer is. For \code{alter_BG_measurement} it can be an \code{integer} referring to a row in the \code{BioGasData} layer of a \code{BGF}}

\item{feedback}{\code{logic}, if \code{TRUE} the function will print a feedback to the R console}

\item{reactor_id}{a \code{character} identifying a the reactor/ fermentation whose entry in \code{BioGasData} needs to be changed}

\item{time_id}{a \code{numeric} specifying the time that has passed since fermentation start for the choosen reactor}

\item{col}{a \code{character} specifying the name of the column of the \code{BioGasData} layer that needs to be changed}
}
\value{
a \code{BGF}
}
\description{
A set of functions that allow to alter data in each layer of a \code{BGF}.
}
\details{
The two functions \code{alter_whatever} and \code{alter_BG_measurement} can be used to change information in each layer of a \code{BGF}.
The function \code{alter_whatever} can be used to change data in the \code{ExpParam} or \code{metaData} layer.

The function \code{alter_BG_measurement} changes a single entry in the \code{BioGasData} layer of the \code{BGF}.
This entry can be selected by providing the name of the reactor for which a measurement should be changed, together with the time after fermentation start when the measurement was acquired.
Alternatively, the row number in the \code{BioGasData} layer of that measurement can be provided to 'ID'.
}
\examples{
# create an example BGF
myBGF <- from_AMPTSV2_report(
        ReactorLayout = c("2*Blank","Cellulose","3*neg ctrl","3*FR1","3*FR2","3*FR3"),
        BlankLabel = "Blank",
        name = "Test",
        InocToSubRatio=2,
        ProcessTemp = 52,
        path = base::system.file("extdata","AMPTSV2.csv",package = "BGFanalyzeR")
        )

# change name to 'newName'
myBGF <- alter_whatever(myBGF,"ExpParam","name","newName",feedback=TRUE)

# exclude reactor 5,6 and 7
myBGF <- alter_whatever(myBGF,"metaData","Excluded",TRUE,c("R5","R6","R7"),feedback=TRUE)

# exclude all reactors
myBGF <- alter_whatever(myBGF,"metaData","Excluded",TRUE)

# change the 'product' column of reactor 1 at time 0 to 7777
myBGF <- alter_BG_measurement(myBGF,"R1",0,"production",7777)

#  do the same for reactor 2 but index via 'ID'
myBGF <- alter_BG_measurement(myBGF,ID=2,col="production",measurement=7777)

}
