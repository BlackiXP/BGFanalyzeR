% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_export.R
\name{save_BGF}
\alias{save_BGF}
\alias{save_ExpParam}
\alias{save_metaData}
\alias{save_BioGasData}
\title{Save a \code{BGF} to an external file}
\usage{
save_BGF(
  x,
  opath = NULL,
  mkdir = FALSE,
  feedback = FALSE,
  append = FALSE,
  format = "csv",
  ...
)

save_ExpParam(x, opath = NULL, mkdir = F, feedback = F, append = F, ...)

save_metaData(x, opath = NULL, mkdir = F, feedback = F, append = F, ...)

save_BioGasData(x, opath = NULL, mkdir = F, feedback = F, append = F, ...)
}
\arguments{
\item{x}{a \code{BGF} that should be exported}

\item{opath}{a \code{path} pointing to a location on the system, where the \code{BGF} should be saved}

\item{mkdir}{\code{logic}, if \code{TRUE} a new directory is created as specified in \code{opath}}

\item{feedback}{\code{logic}, if \code{TRUE} the function will print a feedback to the R console}

\item{append}{\code{logic}, if \code{TRUE} the function assumes that the \code{path} specified in \code{opath} already includes a file name with an approriate ending as specified in \code{format}. Otherwise a file name will be generated}

\item{format}{specifies the output generated by the function. Can be either 'csv' or 'RDS'}

\item{...}{further argumnets that can be passed to \link{dir.create}.}
}
\value{
Either a '.csv' or '.RDS' file created of a \code{BGF}
}
\description{
Saves a BGF to either a '.csv'- or a '.RDS'-file.
It exists one end user function, which internally calls three sub functions, if a '.csv'-file is produced.
Otherwise, it acts as a wrapper for \link{saveRDS}.
}
\details{
The main function used to save a \code{BGF} as an external data file is \code{save_BGF}.
The user can choose if the \code{BGF} should be saved as an '.RDS'-file or in a human readable '.csv'-file.
A human readable '.csv'-file is generated by a consecutive call to the three sub-functions \code{save_ExpParam}, \code{save_metaData} and \code{save_BioGasData}.
Each of these functions is designed to write a single layer of the input \code{BGF} to an external '.csv'-file.
}
\examples{
# create a BGF
myBGF <- BGF(LETTERS[1:5],LETTERS[1],"myBGF",52,2,"manual")


# save it as '.csv' file
save_BGF(myBGF)

# save it as '.RDS' file
save_BGF(myBGF,format="RDS")

# remove files
file.remove("myBGF_BGF_object.csv")
file.remove("myBGF_BGF_object.RDS")

# save only the 'ExpParam' layer
save_ExpParam(myBGF)

# remove file
file.remove("myBGF_ExpParam.csv")

# save only the 'metaData' layer
save_metaData(myBGF)

# remove file
file.remove("myBGF_metaData.csv")

# save only the 'BioGasData' layer
save_BioGasData(myBGF)

# remove file
file.remove("myBGF_BioGasData.csv")

}
