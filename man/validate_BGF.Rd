% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validate_BGF}
\alias{validate_BGF}
\alias{update_BGF}
\title{Validation of data integrity}
\usage{
validate_BGF(x)

update_BGF(x, delete_missing_BGD = FALSE)
}
\arguments{
\item{x}{a \code{BGF}}

\item{delete_missing_BGD}{\code{logic}; defaults to \code{FALSE}. If \code{TRUE} fermentations/ rows in the \code{metaData}-layer with no data in the \code{BioGasData}-layer will be removed from the \code{metaData}-layer.}
}
\value{
a \code{BGF}
}
\description{
A set of two functions, that allow to validate and re-establish data integrity of a \code{BGF}.
}
\details{
The function \code{validate_BGF} can be used to check whether an object is a \code{BGF} or not.
It will raise an error if an object does not fulfill the criteria of a \code{BGF}.

The function \code{update_BGF} checks and updates several parameters, so that the internal logic of a \code{BGF} is consistent.
First of all, it checks if all all fermentations in the \code{metaData}-layer occur in the \code{BioGasData}-layer of a \code{BGF} and vice versa.
It than adds a row to the \code{metaData}-layer, if it detects any fermentation in the \code{BioGasData}-layer, that is not yet represented in the \code{metaData}layer.

In the opposite case, a fermentation has an entry in the \code{metaData} but not in the \code{BioGasData}-layer, the user can decide whether this fermentation should be removed from the \code{metaData}-layer or not.

The function will also remove rows that have \code{NA}in the 'reactor' column from the \code{BioGasData}-layer, sort observations in that layer by 'reactor' and 'time' columns and finally generates new valid row names representing the new row number of an observation in the reordered \code{BioGasData}-layer.
}
\examples{
# create an example BGF
myBGF <- new_BGF("myBGF",52,2,LETTERS[1:15],"A","manuel")

# check if 'myBGF' fulfills the criteria of being a BGF
validate_BGF(myBGF)

# print myBGF
myBGF

# updating the BGF will remove observations from `BioGasData`-layer
myBGF <- update_BGF(myBGF)

# print myBGF again
myBGF

}
